{
  "name": "puhelinluettelo_backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "janjok",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../fullstackopen/osa2/puhelinluettelo && npm run build && cp -r dist ../../../osa3/puhelinluettelo_backend",
    "deploy:full": "npm run build:ui && gid add . && git commit -m uibuild && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
